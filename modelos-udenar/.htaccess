RewriteEngine On

# Evita que se aplique la regla si piden un archivo real (CSS, JS, Imagen)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirige todo a index.php pasando la ruta como un parámetro llamado 'url'
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]